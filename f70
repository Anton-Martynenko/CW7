function alternate(n, firstValue, secondValue){
  let result = [];
  for (i = 0; i < n; i++) {
    if (!(i % 2)) {
      result.push(firstValue);
    } else {
      result.push(secondValue);
    }
  }
  return result;
}
